spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
  application:
    name: Config-Server
server:
  port: 8888

